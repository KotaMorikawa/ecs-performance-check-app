import { ClientSidePresentational } from './presentational';

// Server Component（最小限のプレースホルダー）
// クライアントサイドフェッチでは、実際のデータ取得はクライアント側で行う
export function ClientSideContainer() {
  return <ClientSidePresentational />;
}